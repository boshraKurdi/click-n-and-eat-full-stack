export type TOrder = {
    firstName: string,
    lastName: string,
    bankNumber: number,
    address: string,
    phone: number,
    bankPassword: string,
    priceTotal: number,
    type: string,
    items: number[]
}
